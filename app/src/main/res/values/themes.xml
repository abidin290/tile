<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Theme dasar untuk app.
        Tidak ada Activity launcher, jadi theme ini
        hanya dipakai oleh system (icon di app info settings).
    -->
    <style name="Theme.QuickTile" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">#1A73E8</item>
        <item name="colorPrimaryDark">#1558B0</item>
        <item name="colorAccent">#34A853</item>
    </style>

    <!--
        Theme transparan untuk ScreenshotActivity.
        Membuat window tidak terlihat sama sekali oleh user.
        Tidak ada background, tidak ada title bar.
    -->
    <style name="Theme.Transparent" parent="Theme.AppCompat">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

</resources>
